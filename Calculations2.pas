// Using constant (Math operators)
const 
  a = 121;
  b = 363;

begin
  writeln('Operators:');
  writeln;
  writeln(a,' + ',b,' = ',a+b);
  writeln(a,' - ',b,' = ',a-b);
  writeln(a,' * ',b,' = ',a*b);
  writeln(a,' / ',b,' = ',a/b);
end.