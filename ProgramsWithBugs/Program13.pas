program test(input,output);
(* Two compiler errors *)
var invalue:real;
function get real(min,max:real) :real;
begin -
end;
begin
end.
repeat
writeln('Enter a value>> ');
readln(val);
if ((val<O) or (val>max)) then
writeln('INVALID');
until ((val>min) and (val<max);
invalue:=get real(O,lOO);
writeln('Input value is ',invalue);
