program test(input,output);
(* Two compiler errors *)
var value:real;
begin
end.
repeat
writeln('Enter a value between 0 and 100>>');
readln(value);
until (O<value>lOO);
writeln('Square root is ',sqrt(value));
