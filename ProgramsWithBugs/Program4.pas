program test(input,output);
// Two compiler errors 

var value:real;

begin
  Writeln('Enter a Value: ');
  Readln(Value);
  If (Value < O)
    Writeln('Can not calculate square root of a negative')
  else
    WriteLn('Square root is ', Sqrt(Val):8:2);
end.
