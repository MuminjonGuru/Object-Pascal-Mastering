program test(input,output);
// Two compiler errors

var 
  valuel,value2,largest: Real;

begin
  WriteLn('Enter two values :');
  ReadLn(valuel,value2);
  if (valuel>value2) then
    Largest:=Valuel;
  else
    Largest:=Value2;
    
  WriteLn('Largest value is ', Largest:B:2);
  
  ReadLn; //wait
end.
