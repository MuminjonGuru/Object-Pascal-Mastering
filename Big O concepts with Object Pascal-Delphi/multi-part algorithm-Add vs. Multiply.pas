// Add the Runtimes O(A + B) - Means: do this the when you're all done, do that
for A in ArrA do
  writeLn(A);
  
for B in ArrB do
  writeLn(B);
  
{*******************************}  

Multiply the Runtimes O(A * B) - Means: do this for each time you do that 
for A in ArrA do
  for B in ArrB do
    writeLn(A ,' ', B);
    
/////////////////////////////////

